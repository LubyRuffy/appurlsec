package com.tencent.cloudsdk;

import java.util.List;

// compiled from: SourceFile
public class cl extends ci {
    public List f;

    public boolean a() {
        return (System.currentTimeMillis() > this.d ? 1 : (System.currentTimeMillis() == this.d? 0 : -1)) > 0;
    }
}